# LC7 收尾验收（2026-09-11）

本轮验收完成，当前全部1295项用例已覆盖通过；五模型实网工具与摘要、实际Agent帮助/切换和图内工具回填均通过，合计62次HTTP。当前实施授权及最终状态见[STATUS](../../docs/STATUS_REACT_REBUILD.md)。未提交或推送Git。

## 实际交付

- 新会话默认采用已有精确型号`gpt-5.6-sol-cdx`，保留Responses映射与既定应用容量；已有会话继续恢复保存的模型与思考选择，可用`/model 3`切换。
- 自然语言与`/help`读取同源命令、模型菜单、有效思考设置、能力入口及当前任务状态。默认Sol实际发送low，帮助显示“low（应用默认）”；无任务或取消后也传递最新空状态，旧摘要不能覆盖。
- 沿当前导航同步主说明、README、目录边界、通信与RTO使用说明中的旧入口、容量、持久化和重试描述。未发现需删除的剩余生产路径；没有删除文件或增加依赖，原数据、配置、凭据、用户会话及既有证据保持。

## 本地验证

| 验证 | 结果及计数口径 |
| --- | --- |
| 全仓回归，单独隔离完整CDU用例 | 1293通过，251.27秒 |
| 有效思考显示修正后的帮助/Agent/CLI验证 | 78通过，8.41秒；其中仅1项后补用例未包含在上述1293项，其余重叠 |
| 完整真实CDU工作流 | 1通过，612.31秒；仅远端模型HTTP为替身 |
| 最终覆盖核对 | 三份JUnit按完整用例标识去重后1295项，与当前全部收集用例一致；无失败或跳过 |

全src/tests Ruff与161个源文件mypy通过；36个累计改动Python文件格式、11份Markdown中的104个本地链接/全部围栏及差异检查通过。

完整CDU验收使用冻结的单变量问题，执行M2搜索及全部3个入围候选的M4复核，最终status=success，产生10份真实CDU物理manifest。外部配置改变后仍使用原Problem；SQLite重开、结果查询和重复确认没有新增模型请求或物理重算。保持同一static_ref而篡改回执、删除或损坏物理manifest、取消后篡改last_result均明确拒绝。

## 实网验证

五个精确请求型号保持既有Chat/Responses映射。20种不同有效请求配置均完成真实SSE工具调用、随机合成标记回填和生产解析器续答；default/on等同参数按本地别名去重。五个默认型号使用官方摘要公共接口，人工核对保留允许温度、排除压力、未确认且未执行的含义。实际Agent帮助/模型切换5轮通过，8次本地模型选择没有请求。五模型实际图内工具回填后仍能准确回答随机标记和当前型号，完整工具组及末条应用状态均保留；Flash额外2请求核对正文统计口径。全部62次HTTP为200。逐项安全记录见[实网验收](lc7_live_acceptance_20260911.md)。

## 失败、证据与限制

首轮领域回归376通过、12失败，原因是旧夹具按末条消息或固定消息数定位工具结果，新增程序状态改变了该位置；改为按角色、工具call_id或完整历史序列定位后396项领域回归通过。完整CDU首轮也在同类准备夹具处失败（0.30秒），尚未计算；修复夹具后本轮唯一实际计算运行通过。独立复审发现默认CDX实际发送low却显示模型默认，已从请求参数派生显示并回归。图内Flash补验的额外“整轮流式正文等于最终答复”断言首次失败；单独实网复验确认工具调用消息有两个换行，完整流与所有保存AI正文一致，最终输出与无工具AI正文一致，无丢字或重复。原始比较与复验同时保留，没有为此修改生产语义。未隐去失败，也未把历史通过数计入本轮。

本地JUnit及日志位于`/private/tmp/petroleum-rto-lc7-full.*`、`/private/tmp/petroleum-rto-lc7-help-final.xml`及`/private/tmp/petroleum-rto-lc7-real-workflow-final.*`；真实产物位于后者同名目录。这些是临时运行证据，可能被系统清理；本报告及同目录安全实网记录作为长期验收摘要。

实网仅使用必要的手写合成输入；未测最大上下文窗口、内置搜索、长期稳定性或所有自然语言问法。完整CDU属于合成工程仿真，不是现场验证。当前实现没有开启自动模型fallback。

后续按[使用说明](../../docs/domain_model/01_聚合式垂域模型综合说明.md)启动使用，并根据可复现反馈处理问题；Git发布尚未执行。
