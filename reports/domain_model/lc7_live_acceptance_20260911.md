# LC7 五模型实网验收（2026-09-11）

北京时间 10:15–10:33，通过当前生产解析器及真实 ReactAgent 完成 **62 次 HTTP 请求，全部 200，无重试**。五模型的受支持配置、工具续答、默认摘要及应用帮助检查通过。保留了一项探针比较口径错误及其复验记录，详见下文。逐项参数、结果、响应型号和源码 SHA-256 见[安全证据 JSON](lc7_live_acceptance_20260911.json)。

| 精确请求型号 | 保留的接口 | 不同实际请求配置 | SSE 工具往返 |
|---|---|---|---|
| qwen3.8-max-0902 | Chat | 默认开启、关闭、low、medium、xhigh | 5/5 |
| kimi-k3 | Chat | 默认、low、high、max；不能关闭 | 4/4 |
| gpt-5.6-sol-cdx | Responses | 默认 low、关闭、medium、high、xhigh、max | 6/6 |
| deepseek-v4-pro-0813 | Chat | 默认开启、关闭、high、max | 4/4 |
| deepseek-v4-flash-0731 | Chat | 关闭 | 1/1 |

按生产 `ModelSelection.parameters()` 对相同请求去重，例如 CDX 默认与显式 low、Flash 默认与关闭；20 种配置覆盖全部受支持强度和不同外发参数。每种配置先请求一次工具，再生成此前未知的随机标记，以原始调用 ID 回填工具结果；20 次续答均精确返回标记，使用当前生产 SSE 解析器和完整原生消息续接。生成预算保持 16,384。CDX 响应声明型号为 `gpt-5.6-sol`，Pro 为 `deepseek-v4-pro-ga-260813`；其余与请求 ID 相同。此处记录网关声明，不独立证明后端身份。

请求计数与检查范围如下：

- **40 次：** 上述 20 组原生工具往返，校验完整调用、参数、结束帧、续接和随机标记。
- **5 次：** 五模型默认配置调用生产 `SummaryModel` 与官方公开 `SummarizationMiddleware.before_model`，不预裁输入，并保留当前输入。程序校验摘要非空、无工具调用及当前输入保留；另逐份人工阅读，确认允许温度、排除压力、尚未确认且未执行均保留，没有产生新授权。两种校验口径分开记录。
- **5 次：** 真实 Agent 从 CDX 开始，随后分别从 CDX 切到其余模型；8 次本地选择均不请求网络。五轮回答均说明当前完整 ID、正确思考状态、原会话随机标记及 `/help`、`/model`、`/resume`。CDX 为 low 应用默认，Flash 关闭。没有领域工具调用，切换后没有把旧模型原生 assistant/reasoning 消息外发。
- **10 次补验：** 前述原生矩阵不附加应用状态，帮助轮也不调用工具，因此再以五个默认模型各跑一次真实图工具回填。合成 `get_plant_info` 只生成随机标记，各调用一次；工具回填后请求末尾仍有 `application_help`，五模型均恢复标记和当前 ID，原始完整工具组保留，没有准备方案或计算。
- **2 次 Flash 复验：** 上项 Flash 的“整轮流式正文等于最终答复”比较为 false。代码会显示工具调用消息正文，而最终答复只取无工具调用的消息，该比较口径不成立。原样本未保存具体差字符；追加同一合成用例，实际捕获工具消息的两个换行，确认整轮显示等于全部已保存 AI 正文、最终答复等于无工具调用的 AI 正文，没有重复或丢失。原始 false 及本次公开正文均保留；另有仅本地 MockHTTP 夹具复现该区别，没有修改生产代码。

合计为原有 **50 + 工具回填 10 + Flash 复验 2 = 62**。所有实网只发送手写合成输入、随机标记及正常应用/工具合同文本；领域替身阻止配置读取及正式准备计算。只保留安全结果、公开正文和元数据，不保存原始协议、推理、请求头或凭据。

临时探针位于 `/private/tmp/petroleum-rto-lc7-live`，可能被清理；长期保留的是本报告及 JSON，未新增维护入口。以下为实际运行脚本 SHA-256，其他本地探针和生产文件完整哈希也在 JSON 的 `source_evidence` 中：

| 临时脚本 | SHA-256 |
|---|---|
| acceptance.py | 7768c9e59350c3ce970367ec849fec48178ef4f7cc3a51b6085f5ed8a4870418 |
| agent_help_switch.py | b82096eb70c9779f387cceab912e593d63c7580545eb4404f488710bc6d70db6 |
| agent_tool_roundtrip.py | ecd262e01f05f52642d696e26b468b0b1fe01b02d383265c53aab7b6da1bb351 |
| flash_stream_confirmation.py | 44b9a5ef7504879be0fbb018f9a812b280083ecabb53b37d5f14980020bd6220 |

本次小样本通过不证明长期供应商稳定性、最大上下文窗口或普遍摘要保真。现有 Chat/Responses 映射保持不变，没有重做全模型 Responses 迁移，也没有把这些合成结果作为物理或现场验证。
