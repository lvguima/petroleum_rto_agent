# 源码区领域Agent

_2026-09-11 · 原生工具循环、本机会话恢复、固定审批执行、终端正文流、公共摘要路径与有限重试；批次状态见实施文档。_

## 🚀 当前能力与启动

`rto-chat`和`python -m petroleum_rto.domain_model`装配同一个`ReactAgent`。单一官方`create_agent`图保留原始消息与会话状态，支持一般对话、工艺问答、装置身份、配置工况及已有结果查询；模型工具负责查询、准备、修改和取消，真实用户确认后由固定节点完成静态搜索及动态复核。当前会话保存到本机SQLite，模型正文可在交互终端逐片显示；旧对话按容量摘要，大工具结果可在重启后继续分页读取。

当前开发与测试目录为`/Users/idc/Desktop/petroleum_rto_agent`，开发分支为`feat/react-agent`。旧ba1c目录仅是迁移来源，后续从桌面启动。安装锁定依赖：

```bash
cd /Users/idc/Desktop/petroleum_rto_agent
uv sync --locked --extra domain-model --group dev
uv run --locked --extra domain-model rto-chat
```

日常在桌面项目目录直接运行`.venv/bin/rto-chat`，也可以使用模块入口：

```bash
PYTHONPATH=src .venv/bin/python -m petroleum_rto.domain_model
```

原有密钥原位保留在桌面项目的`src/petroleum_rto/domain_model/dmx_api.json`，不纳入Git。可以是一行密钥或只含`api_key`的JSON；原读取器继续拒绝宽松权限、符号链接、重复键及超限文件。工作树不会从其他位置悄悄寻找密钥；没有配置时启动返回安全错误。

`chat_settings.py`中的启动模型、Chat端点和附加角色说明仍生效；新会话默认使用`gpt-5.6-sol-cdx`，已有会话恢复其保存的模型与思考选择。切换已有会话可输入`/model 3`，这不会清除历史或任务。传输从Chat端点确定API根地址，再按所选模型调用Chat或Responses。`domain-model`可选依赖包含LangChain、httpx和官方`langgraph-checkpoint-sqlite`；后者要求的aiosqlite/sqlite-vec随锁文件安装，本项目不启用向量扩展。RTO和CDU核心不依赖框架或会话数据库。

实际CLI在`runs/assistant/session.sqlite`保存当前会话，持有本机独占锁直到关闭；另一个CLI同时打开会明确失败。数据库及锁文件权限为0600，符号链接和损坏/不支持版本拒绝读取；只保存严格JSON及白名单框架类型，不使用pickle回退，不保存凭据或HTTP客户端。首次启用从新会话开始，不能恢复旧进程尚未持久化的内存。

## 💬 终端命令

交互终端支持中文退格删除、左右光标移动和行内修改；光标在行尾时，`Ctrl-U`清空当前输入，回车提交。输入阶段的`Ctrl-C`退出程序，空行不退出。实现使用Python自带的`readline`行编辑支持；若当前Python缺少该模块，明确提示并退出。文件或管道输入仍按行读取，多行文本仍逐行提交，不改为一条多行消息。

| 命令 | 当前行为 |
| --- | --- |
| `/model` | 显示当前模型和列表；随后可直接回复编号或完整ID选择，输入0退出选择 |
| `/model 2`或`/model kimi-k3` | 切换精确模型，保留会话、快照和最近结果，不触发请求 |
| `/thinking` | 查看当前模型思考设置 |
| `/thinking on high` | 设置当前模型支持的思考模式和强度；Flash渠道拒绝开启，非法强度拒绝 |
| `/thinking off`或`/thinking default` | 关闭或恢复该模型默认；常开模型拒绝关闭 |
| `/capabilities` | 本地读取装置与能力，展示结果并加入后续上下文 |
| `/result [workflow_id]` | 查看受控结果编号或当前会话已有结果；不接收任意路径 |
| `/confirm` | 本地确认已展示的当前版本，通过公共审批节点顺序执行M2/M4；已完成时校验已有证据并展示结果 |
| `/resume` | 恢复已批准但未完成的任务；先展示恢复摘要，不批准新方案，不因普通追问续算 |
| `/cancel` | 取消后续执行，保留已有结果 |
| `/clear` | 清除会话、摘要、分页原文、方案、快照和最近结果，保留模型选择及磁盘结果 |
| `/help`、`/exit` | 查看包含当前模型/思考/待办的帮助、退出并关闭HTTP/SQLite资源及会话锁 |

自然语言询问“你能做什么”“当前用哪个模型”“怎么改思考设置”仍由同一个Agent回答。每次请求都附加与`/help`同源的命令说明、模型菜单、当前思考及任务状态，不依赖旧摘要记住应用用法。切换模型、修改思考或取消方案后立即重建该视图；没有方案也明确传递空状态，旧历史不能恢复确认资格。应用帮助本身不读取装置配置，目标和变量的实际能力仍由`/capabilities`或领域工具提供；模型不能通过回答自行切换设置。

普通输入不进入固定意图分类。只有无参数`/model`开启一次模型选择：下一条编号或精确ID在本地处理，成功后关闭；无效编号提示重试，空行保留选择状态。输入0退出选择；普通自然语言退出选择并正常聊天，其他斜杠命令退出选择并执行原有功能（`/cancel`仍取消优化方案）。未打开选择时，单独数字保持普通聊天；`/thinking`或切换完成后的列表展示不会重新开启选择。正在执行的一轮不能换模型。

默认CLI执行期间按Ctrl-C会请求停止当轮：普通模型和摘要都不会再发起后续HTTP尝试，也不会执行中断请求返回的新工具；已完成查询保留，返回输入后可以继续。仍可能需要等待当前HTTP请求、重试退避或工具调用结束，不承诺立即终止底层计算。每次图运行使用独立的官方`RunControl`，回调异常退出也会先请求停止再关闭；运行结束后恢复原信号处理器，不覆盖嵌入调用方的自定义SIGINT处理。这些控制不写入会话，也不改变后续明确恢复任务的规则。

切换至Flash时固定关闭思考，切换其他模型时默认开启；不继承前一模型的思考设置。Flash的`/thinking on`直接说明当前渠道仅使用非思考模式，不发送请求、不改会话或方案。其余模型仍支持各自合法设置，Kimi不能关闭思考。

交互终端在执行期间逐行显示程序进度：读取配置工况、准备方案、M2搜索点评价、M4入围候选复核及阶段结果。M2显示实际已评价点数，不用搜索预算冒充总数或百分比；M4显示已复核数/完整入围数。搜索点与最终方案明确区分，无可行解和评价错误分别报告，已有阶段复用会注明未重新计算。进度不进入模型对话，也不在最终输出中重复打印。

输入编辑期间不后台刷新；管道/重定向继续保留按行输入和最终结果输出，不显示交互进度或终端控制序列。交互终端消费标准消息片段显示模型正文，程序进度、摘要响应与原始推理不混入正文，最终输出不重复打印已显示内容；文字显示后若失败，会明确标记本次回答不完整。

当前模型遇到可恢复的网络、限流或服务错误时，普通请求最多尝试3次，不自动换模型。若上次已显示文字，重试前会说明回答未完整生成并标明第2/3次尝试，随后显示重新生成的正文；失败片段不会拼入保存的成功回答。只接收正文回调的客户端也有明确程序分隔。三次仍失败时报告实际尝试次数和安全错误原因，已完成查询、方案和结果保留，不把错误伪装成模型答复。认证、权限、解析或已收完传输但协议不完整等永久错误直接停止；未完整接收的工具调用不执行。

## 🔧 优化交互与恢复

工具合同升级为3.0.0，删除模型可调用的manage/solve/verify入口，提供独立`cancel_optimization`。准备方案仍不接受`constraints`字段，系统门禁由程序自动保留；额外业务限制尚不支持，模型应如实说明。`max_candidates`是最终返回方案总数，默认1，上限按能力中的对应路线`top_k`；33/81等内部搜索预算不是该参数。只选择用户允许的available变量即可，无需包含deferred回流比。字段出错时，工具返回具体issues供模型纠正，不应让用户通过换目标或加入不可用变量排查程序错误。

1. 直接描述目标和可调变量。模型先读取能力与工况，用`prepare_optimization`确定性构造问题，程序展示目标、变量范围、约束、工况时间与执行范围；此时没有仿真。
2. 回复“只调温度，不调压力”等修改，模型重新准备方案并产生新版本。旧版本立即失去确认资格；修改失败也不能裸确认旧方案。
3. 程序通过公共`interrupt`节点等待后续用户确认。整条`/confirm`、“确认”或“确认执行”均由本地真实输入转换成同一个`Command(resume=...)`审批动作，检查完整输入、当前轮次和已展示版本；审批本身无需模型请求。
4. 审批后图固定完成M2静态搜索及配对评价，再对完整入围列表做M4复核并最终选择。模型不能决定阶段顺序、跳过M4或挑选复核子集。完成后先展示推荐调整、基准与预测、改善幅度及备选方案，再由当前模型自动补充文字说明。

结果展示来自已核验回执，按“推荐调整 → 预期效果 → 其他候选 → 工况与结果编号”组织，不再直接打印JSON；它是仿真优化建议，不是策略库中已发布的策略。常规数值保留两位小数，K同时显示℃，比例显示百分数、比例差显示百分点；很小的非零变化保留必要精度，原始JSON与物理证据不舍入。无可行方案、评价错误及未通过发布门槛分别说明。

自动说明复用同一Agent和当前模型，只发送已展示的报告，关闭工具，并拒绝返回的工具调用。正常只增加一次模型请求，临时失败沿现有机制最多尝试3次；说明失败或中断不会撤销结果、重跑M2/M4。`/resume`完成后同样自动说明；`/result`及重复确认只校验并展示已有结果，不再次请求模型。交互终端先显示报告，再流式显示说明，避免回合结束时重复打印。模型文字仍可能出现表述偏差，关键数值以程序报告和保存的回执为准。

三个优化目标是同一`prepare_optimization`工具的`objectives`参数选项：有价值馏分收率、单位进料炉燃料热负荷代理及产品质量代理最大相对偏离。目标与方向登记在[能力目录](../../configs/rto/capabilities/catalog.json)，计算公式位于[formulas.py](../../src/petroleum_rto/rto/evaluation/formulas.py)；`get_plant_info`读取这些能力供模型选择，没有三个各自独立的求解工具。

重复强调变量排除时，即使方案已有同样范围，也要求模型重新准备并由程序展示完整范围。新方案等待本轮结束后展示，随后才接受下一轮确认；这不叫“方案失效”。程序用简短状态提示说明下一步。

待确认时聊天、查询和切换模型保留当前方案，不再依赖keep工具。进入修改路径即撤销旧资格，参数或业务校验失败后仍须重新准备并展示；取消后旧动作不能复活。改变方案的工具调用必须独占一条模型响应，混排批次在执行前拒绝；领域工具保持串行。

确认规则只忽略首尾空白，不忽略标点、引用及附加条件。程序从实际当前用户输入判断，模型没有确认工具，也不能截取或复述“确认”来授权。“同意”“确认？”及“确认但只调温度”均不能授权；其他自然语言仍走统一工具循环，没有独立意图分类器。

若DeepSeek Pro思考模式的历史回复缺少必需推理字段，下一次带工具请求会在发送前停止并提示；可以显式`/thinking off`或切换模型后继续，历史转为背景数据，方案与结果保留。Flash仅使用已验证的非思考模式，停止适配该渠道的思考续接；这不代表原厂Flash不支持思考。

重启会恢复原始消息、模型/思考选择、快照、固定方案、审批与阶段状态、摘要和分页，但只展示恢复摘要，不请求模型或运行计算。待审批方案仍须确认；已批准未完成方案必须在展示后明确输入`/resume`，普通追问不续算。`/cancel`取消任务并保留已有结果；`/clear`清除当前会话全部可恢复记录和授权，保留模型选择与磁盘RTO证据。

固定方案保存完整能力包、Intent、Context和Problem，通过严格版本化codec从自身内容重建同一问题；当前配置更改或删除不会替换原绑定工况。已有M2/M4或取消后保留的最近结果在恢复时从磁盘严格验真，缺失或损坏明确失败，不能仅凭会话回执宣称完成。完整阶段复用不新增仿真；半个阶段中断仍沿用原编排语义，不保证每个候选恰好执行一次。原回执的`physical_m2_executions`/`physical_m4_executions`是历史调用计数，恢复只检查其合法形状，不重新证明历史次数，也不当作本次新增计算。

## 🔌 P0模型与接口合同

完整ID是唯一键；下表记录当前保留的协议及2026-09-11 LC7实网范围。20种不同有效参数配置均通过真实SSE工具往返和生产解析器，五个默认型号的官方摘要也通过。完整记录见[实网验收](../../reports/domain_model/lc7_live_acceptance_20260911.md)，数值与可执行参数以[models.py](../../src/petroleum_rto/domain_model/models.py)为准。

| 精确ID | 候选端点/思考方式 | 当前限制 |
| --- | --- | --- |
| `qwen3.8-max-0902` | Chat；`enable_thinking`，开启时保留推理；支持low/medium/xhigh | 默认、关闭及全部登记强度的当前Chat流式工具往返通过；默认摘要通过，最大窗口未测 |
| `kimi-k3` | Chat；始终思考，low/high/max；完整reasoning_content续接 | 关闭在本地拒绝；默认及全部登记强度的当前Chat流式工具往返通过，默认摘要通过 |
| `gpt-5.6-sol-cdx` | Responses；reasoning.effort与summary独立于显示 | 新会话默认型号；关闭及全部登记强度的当前Responses流式工具往返通过，默认摘要通过；应用预算保持256K |
| `deepseek-v4-pro-0813` | Chat；`thinking.type`开启/关闭，合法强度按配置 | 默认、关闭及全部登记强度的当前Chat流式工具往返通过，默认摘要通过 |
| `deepseek-v4-flash-0731` | Chat；固定`enable_thinking=false` | 当前Chat非思考流式工具往返及摘要通过；开启在本地拒绝，不补偿该渠道思考缺字段 |

DMX的[接口选择指南](https://doc.dmxapi.cn/omp.html)明确支持Responses，同时将GPT推理工具调用推荐到Responses、DeepSeek/Qwen/Kimi推荐到Chat Completions。当前保留按模型分配的两种协议；平台支持Responses不代表所有精确模型的工具与推理组合已验证可统一迁移。

来源：[DMX原生Chat工具](https://doc.dmxapi.cn/function-call.html)、[DMX Responses工具](https://doc.dmxapi.cn/res-function-call.html)、[DMX GPT-5.6](https://doc.dmxapi.cn/GPT-5.6.html)、[DMX DeepSeek](https://doc.dmxapi.cn/thinking-deepseek.html)、[千问型号表](https://platform.qianwenai.com/docs/developer-guides/getting-started/text-generation-models)、[Kimi K3](https://platform.kimi.ai/docs/guide/kimi-k3-quickstart)、[Kimi模型表](https://platform.kimi.ai/docs/models)、[DeepSeek当前版本](https://api-docs.deepseek.com/quick_start/pricing/)。

GPT Sol CDX的256K按256,000 tokens计，包含生成预留；此设置不改变其他模型的预算、协议或思考模式。既有P6探针仅作历史证据；LC7使用当前生产解析器重新验证，并记录请求精确ID和供应商自报别名的区别。实网小样本不证明最大窗口、内置搜索或长期稳定性。

### Responses统一迁移实测（2026-09-09）

2026-09-09的历史探针使用合成标记测试五个精确型号的标准Responses输入、SSE及两次依赖工具调用。GPT CDX与Flash非思考通过生产解析器和完整往返。Qwen、Pro在诊断路径原样续传其返回项后完成往返，但reasoning没有非空encrypted_content，面向GPT的生产Responses解析器拒绝；这是适配差异，不能据此声称模型不支持Responses。Kimi对字符串及标准消息数组均报400 messages must not be empty；对合法函数名read_marker和lookup报400 function name is invalid，未通过工具接口。因此“全部通过后统一”的条件未满足，协议映射保持，未增加Chat回退或伪造推理字段。该次未验证全强度、长上下文或内置搜索。

## 🧩 模块与消息

- [native.py](../../src/petroleum_rto/domain_model/native.py)适配LangChain模型，使用httpx直接保留完整Chat assistant或Responses output。现成循环由LangChain提供，不自行解析文本工具计划。
- [react.py](../../src/petroleum_rto/assistant/react.py)装配单一create_agent图及公共审批/M2/M4中间件节点，管理本地命令、恢复、普通模型有限重试与正文/进度显示；原始messages和session是唯一活动状态。
- [presentation.py](../../src/petroleum_rto/assistant/presentation.py)将严格读取的结果回执转换为可读方案，仅处理显示精度，不修改机器合同或计算值。
- [state.py](../../src/petroleum_rto/assistant/state.py)定义严格版本化会话、模型选择及上下文恢复；[session.py](../../src/petroleum_rto/assistant/session.py)管理官方SqliteSaver、严格JSON、单会话互斥和关闭。
- [native_tools.py](../../src/petroleum_rto/assistant/native_tools.py)提供五个无独立活动状态的领域工具，参数模型生成Schema并严格校验，状态从ToolRuntime传入并通过Command更新。
- [context.py](../../src/petroleum_rto/assistant/context.py)根据图状态重建发送视图与原文覆盖关联，提供第六个工具`read_tool_result`；通过官方`SummarizationMiddleware.before_model`公共接口逐批摘要，有效批次先保存检查点。每次模型调用前投影当前方案，摘要不能授权，原始messages始终完整保留。
- [models.py](../../src/petroleum_rto/domain_model/models.py)提供精确ID与实际请求参数；请求模型和服务端声明模型分别记录，后者不等于独立身份认证。

Chat响应允许无正文的工具调用。完整调用ID、名称、JSON参数和推理字段保留，下次附带对应tool结果。Responses完整保留函数调用、消息和加密推理项，回传function_call_output；不依赖previous_response_id或跨模型响应链。

`_stream`提供标准`ChatGenerationChunk`/`AIMessageChunk`，`_generate`聚合同一路径；正文可逐片显示，完整工具调用和原生推理状态只在严格结束校验后提交一次。Chat参数增量按索引拼接，Responses核对completed事件与完整output；截断、重复JSON键、非法类型、缺少必要推理或错误结束原因都会停止，不执行不完整参数。`usage_metadata`只映射实际返回的完整用量，缺失不伪造；实际外发计数在传输边界累计。关闭推理显示不会删除必要续接状态。

## 🗂️ 容量与保留

完整原始记录由图和本机SQLite保存，发送视图按当前模型重建。切模型时旧对话/工具结果作为有来源的背景数据进入新上下文段；不伪造新模型的assistant推理，不丢快照引用。没有64条消息提交上限，也没有第二套活动历史。

当前使用保守UTF-8字节估计加协议余量，包含工具Schema和推理状态；再预留应用生成预算。该估计可能比真实token数量大，不是精确tokenizer。GPT Sol CDX采用用户指定的256K应用上下文预算，其他模型保留既有预算；按预算自动摘要和检查超限，不再存在未知容量禁止请求路径。

接近容量时，系统通过官方摘要组件的公共钩子，使用当前模型分批摘要较早的完整交互。公开`keep`参数决定保留量，实际工具组切点由组件处理；当前输入和本轮工具交互不参与摘要。`trim_tokens_to_summarize=None`关闭摘要前预裁剪，过大历史通过缩小每批较旧前缀处理，原始记录不删除。官方消息更新只应用到一次性发送视图，不改写图中的原始messages。

每批摘要必须非空、无工具调用，并让完整协议载荷变小，才提交摘要及原文覆盖关系；有效批次先保存检查点，再继续下一批。后续失败不会覆盖此前摘要，失败批次的预算消耗也会保存。`covered_until`仅从官方返回的保留后缀推导，用于关联已归档原文；已删除私有切点调用和摘要模型改写。摘要不能恢复确认资格，终端只提示已生成摘要，摘要响应不混入模型正文。

大工具结果以明确的全文引用替代发送内容，`read_tool_result`按字符偏移分页，不重做原计算；全文引用及原文可跨进程恢复。切换模型保留摘要及原文关联，旧模型推理字段不跨模型发送；重启恢复原始消息、有效摘要、分页和方案，启动仍零模型请求、零计算。`/clear`删除摘要和分页原文；没有历史会话列表、自动淘汰或无限容量承诺。单条不可拆分历史、当前输入/工具批次或必需任务状态仍超限时停止并保留记录。完整流程见[架构说明](../architecture/02_领域智能体架构与功能设计.md)。

每轮模型调用次数是循环资源预算，与窗口和历史轮数不同。普通模型通过官方`ModelRetryMiddleware`只重试结构化临时错误，循环和退避由框架处理；公共包装只观察真正开始的尝试。上下文先生成发送视图，普通请求重试不重跑摘要或已完成工具。摘要另有每轮逻辑批次预算，复用相同上限值，不占用Agent循环次数；摘要模型通过公开`with_retry`执行自己的有限重试，不再叠加普通模型中间件。永久错误、解析错误、空摘要或工具调用响应不重试；达到预算、摘要无效或不缩小载荷时停止，保留原文与已提交摘要。

普通逻辑调用与摘要逻辑批次各自最多`max_model_calls`次，两条路径共用每次最多3次尝试的常量，因此每轮普通与摘要HTTP尝试分别不超过逻辑上限的3倍，合计不超过`6 * max_model_calls`。上限由现有预算推导，没有新增独立配置或重复计数器。传输的`request_count`在实际外发边界核对所有尝试，发送前的容量调整不产生HTTP尝试。HTTP超时及响应字节保护独立于上下文，响应上限按本次生成预算计算。自动换模型关闭，也不加载外部追踪配置。

## 🧪 验证与迁移边界

本地验证使用httpx替身和真实独立进程，覆盖实际工具协议、消息/模型/方案/全文恢复、配置变更后固定问题重建、启动零请求零计算、并发拒绝、清除旧授权、阶段错误、中文分片与中断及重复显示。公共摘要路径进一步覆盖有效批次持久化、后续批次失败保留已提交状态，以及同模型和切换Kimi后的跨进程续接；完整工具组、原始记录和分页仍受保护。有限重试的本地用例覆盖临时错误三次上限、永久错误单次、摘要不倍增、已完成工具不重复、流式重试分隔及失败后状态保留。本轮范围与结果以项目状态为准。测试证明程序行为，不证明真实模型的自然语言理解、工具选择或数值忠实程度；本轮五模型实网记录使用当前生产解析器，不能用历史探针替代本轮结果；其范围与未测边界见验收报告。

旧Chat、分类/确认适配器、四动作网关和Agent运行时已删除；LC2–LC4进一步移除重复活动账本、manage/keep协调及模型自由调用M2/M4的入口。LC5摘要路径已移除私有切点与摘要模型改写，改用公共接口、逐批检查点和受限摘要重试。LC6已接入官方普通模型有限重试、真实尝试进度、派生HTTP上限、流式重试分隔及中断后停止后续请求；当前请求、退避与底层计算仍沿受支持边界结束。原凭据文件校验与编码泄露保护继续用于新传输。独立D0公共合同、RTO CLI和v4证据格式保持，固定方案codec与会话状态独立版本化；本批没有新增依赖或修改物理模型。LC7已完成本轮总验收：应用帮助、默认Sol及现行说明已同步，当前全部用例与必要实网检查通过；具体范围、证据和未测边界见[总验收报告](../../reports/domain_model/lc7_final_acceptance_20260911.md)。

实施证据、未验证项与下一步以[STATUS_REACT_REBUILD.md](../STATUS_REACT_REBUILD.md)为准，旧[STATUS.md](../STATUS.md)保持历史基线角色。
