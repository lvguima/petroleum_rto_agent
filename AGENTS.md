# Codex 项目工作协议

## 文件职责

- 本文件只保存长期有效的 Agent 工作规则、质量门禁和交接要求。
- 当前实施进度的唯一事实来源是 `docs/modeling/STATUS.md`。
- 建模范围、技术路线和 M0～M7 验收条件以
  `docs/modeling/01_CDU_Mini_Loop实施方案.md` 为准。
- 工艺背景和数据可信度以 `docs/01_常压蒸馏工艺及数据分析.md` 为准。
- 不在本文件记录实时完成情况、临时问题、某次运行结果或下一项具体开发任务。

## 开始工作前的强制流程

开始任何实质性工作前，必须依次完成：

1. 阅读 `docs/modeling/STATUS.md`，确认当前阶段、执行门禁、阻塞项和下一步。
2. 阅读实施方案中与当前阶段有关的范围、接口和验收条件。
3. 检查相关代码、配置、测试和运行产物，核对状态文档是否与仓库实际一致。
4. 如果状态文档与仓库事实不一致，以可检查的仓库事实和验证结果为准，并先修正
   `STATUS.md`，再继续实施。
5. 确认工作没有超出状态文档中的“当前授权范围”；需要扩展范围时先取得用户确认。

计划、待办和已创建的文件都不能单独证明任务完成。不得把“计划实施”“代码已写但未运行”
或“测试尚未执行”表述为“已完成”或“已验证”。

## 实施过程规则

- 每次只推进当前里程碑中边界清晰、能够验证的一组工作。
- 遵守实施方案定义的模型边界，不自行加入后续策略库、优化器、垂域模型、智能体、
  DCS 接口或现场控制功能。
- 优先复用现有设计和数据；任何新假设都要记录来源、适用范围和不确定性。
- 不得用现场数据不足解释程序内部的不守恒、非有限值、负库存或错误的收敛状态。
- 未通过当前执行门禁时，不安装依赖、不运行测试、不运行仿真，也不把环境标记为就绪。
- 不覆盖无关的用户修改；发现与当前任务冲突的既有改动时，暂停并说明冲突。

## STATUS 实时更新协议

`docs/modeling/STATUS.md` 必须根据项目的实际进度实时维护，不能只在整个里程碑结束时更新。

出现以下任一情况后，应在同一工作轮次立即更新状态文档：

- 新增、修改、删除了实施相关的代码、配置、测试或文档；
- 某项交付物开始、完成、退回或被阻塞；
- 执行了测试、仿真、检查或校准，并获得新的验证结果；
- 发现状态记录与仓库实际不一致；
- 用户改变了范围、优先级、环境门禁或验收要求；
- 工作即将暂停、交接或结束。

每次更新必须基于当时可以检查的事实，并至少同步以下内容：

1. 最后更新时间和当前里程碑；
2. 当前执行状态及门禁；
3. 本轮实际变更和受影响文件；
4. 已执行的验证、结果和证据；
5. 未执行的验证及原因；
6. 新增阻塞、风险、假设或待决事项；
7. 一个明确、可执行且仍在授权范围内的下一步；
8. 状态变更记录。

状态维护遵循以下判定规则：

- 仓库实际内容优先于旧状态描述；验证证据优先于主观判断。
- 只有满足实施方案验收条件并有验证证据时，里程碑才能标记为“已完成”。
- 代码存在但没有运行，只能标记为“已实现、未验证”或“部分完成”。
- 测试失败必须如实记录，不能删除失败信息后仍保留“已完成”。
- 未执行测试时必须明确写出“未执行”和原因，不能写成默认通过。
- 不使用没有明确计算口径的完成百分比。
- 保留简明的状态变更记录，同时让文档顶部始终反映最新快照。

## 验证与完成门槛

- 环境门禁解除后，先执行与本次变更最相关的最小验证，再逐步扩大验证范围。
- 每个里程碑必须按实施方案中的通过条件逐条确认。
- 守恒、边界值、方向性、收敛失败处理和结果可重复性必须有自动检查或可复核证据。
- 运行产物必须明确区分合成仿真数据与现场数据，不得暗示已经过现场验证。
- 只有代码、测试、文档和 `STATUS.md` 相互一致时，才能宣布本轮工作完成。

## 结束与交接

结束或暂停一轮工作前，必须：

1. 复核本轮涉及的文件和验证结果；
2. 按实际情况更新 `docs/modeling/STATUS.md`；
3. 确认状态中的阻塞项和下一步足以让下一位 Agent 直接继续；
4. 在交付说明中报告已完成内容、未执行验证、风险和下一步，且与状态文档保持一致。
