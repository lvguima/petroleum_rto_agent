# Codex 项目工作协议

## 项目目标与长期边界

- 项目目标是在机理仿真评价能力之上建设目标数量无关（`1..N`）的模块化离线RTO、统一评价器和策略库，并通过严格意图合同连接未来垂域模型，最终形成可被大型智能体组合调用、又能独立替换和验证的部件。
- CDU Mini Loop是首个仿真后端实现，其完成状态以`docs/STATUS.md`为准；真正的后端可替换能力要通过RTO现行主文档定义的中立 `SimulatorPort` 建立，不能提前宣称已经实现。
- 业务目标解释属于用户、生产计划或未来垂域模型；意图解析器只做结构化协商；确定性问题构造器负责生成统一优化问题；特征分析器提取求解所需事实；路由器选择兼容求解插件；求解器负责候选搜索；机理模型只执行结构化仿真请求；统一评价器负责基准配对、KPI和约束；策略库只保存经验证和审核的策略。
- 当前首版变量、评价漏斗、执行方式、非目标和具体禁用项以RTO现行主文档及`docs/STATUS.md`的授权为准，不在本文件复制会随里程碑变化的配置。
- 未来垂域模型必须输出与人工fixture相同的统一严格意图合同，运行上下文由受信调用方独立注入；未来HYSYS必须通过中立仿真端口替换当前适配器。任何上层模块不得直接依赖RTO求解器、CDU或HYSYS内部对象。
- 未获得新的现场证据与正式授权前，所有结果和策略声明均限定为合成工程仿真，不得表述为现场验证、产品放行、实际经济收益、安全边界或可直接下装的控制策略。

## 文件职责与事实优先级

- 本文件只保存长期有效的 Agent 工作规则、质量门禁和交接要求，不记录实时进度、临时问题或某次运行结果。
- 当前实施进度、授权范围、阻塞项和下一步的唯一事实来源是 `docs/STATUS.md`。
- 项目文档导航与目录规则以 `docs/README.md` 和 `docs/architecture/01_项目目录与模块边界.md` 为准。
- 已完成机理模型的现行技术主入口是 `docs/cdu/01_CDU_Mini_Loop机理模型综合说明.md`。
- RTO职责、统一合同与迁移门禁的现行主入口是 `docs/rto/01_RTO系统综合说明.md`。
- 垂域模型与统一RTO之间的请求、响应、结构化修复和用户澄清以 `docs/rto/02_垂域模型与RTO通信协议.md` 为准。
- R0～R11既有交付的设计背景和历史门禁分别记录在 `docs/rto/archive/02_RTO策略库与机理仿真解耦实施方案.md` 与 `docs/rto/archive/05_RTO多目标与垂域意图接口扩展实施准备.md`；它们是统一化迁移的兼容基线，不是按目标数量继续扩展的两条主线。
- 当前统一RTO的长期对象、依赖方向和扩展规则以 `docs/architecture/01_项目目录与模块边界.md` 为准；可执行字段、版本和数值仍以源码与版本化JSON为准。
- 工艺背景、资料来源和数据可信度以 `docs/cdu/01_常压蒸馏工艺及数据分析.md` 为准。
- `docs/cdu/archive/` 只保存历史设计与签署快照，不作为当前规范。归档内容与当前事实冲突时，以源码、版本化配置、自动测试、正式证据、`docs/STATUS.md` 和现行主文档为准。
- 可执行合同、字段、数值和版本以当前源码及版本化JSON为事实来源；Markdown负责解释，不复制维护第二套可执行配置。

## 开始工作前的强制流程

开始任何实质性工作前，必须依次完成：

1. 阅读 `docs/STATUS.md`，确认当前里程碑、执行门禁、授权范围、阻塞项和下一步。
2. 阅读与当前工作相关的现行主文档：机理模型工作阅读CDU综合说明，RTO/策略库工作阅读RTO系统综合说明；只有追溯历史决策时才阅读归档实施方案，需要工艺或数据判断时再阅读工艺分析文档。
3. 检查相关代码、配置、测试和运行产物，核对状态文档与仓库实际是否一致。
4. 如果状态与仓库事实不一致，以可检查的仓库事实和验证结果为准，并先修正 `docs/STATUS.md`，再继续实施。
5. 确认工作没有超出 `docs/STATUS.md` 的当前授权；需要扩展模块、依赖、外部服务、数据用途或现场能力时，先取得用户确认。

计划、待办、文档或已创建但未执行的代码都不能单独证明任务完成。不得把“计划实施”“代码已写但未运行”或“测试尚未执行”表述为“已完成”或“已验证”。

## 架构与实施规则

- 目录采用“类型分层、模块命名空间”：安装源码放在`src/petroleum_rto/<module>/`，配置、数据、测试、脚本、报告和运行产物分别放在各顶层目录的`<module>/`下，项目级文档放在`docs/`，模块文档放在`docs/<module>/`。
- CDU与RTO不得共用未命名归属的业务文件。只有确实跨模块且合同稳定的内容才能进入共享层；不得为了省路径而把模块内部对象提升为公共对象。
- M5～M7历史证据中的`configs/...`、`data/...`、`reports/modeling/...`和`base_files/...`是稳定逻辑资源标识。物理文件迁移由CDU仓库路径解析器兼容，禁止仅为目录美观批量改写历史证据字节或指纹。
- 每次只推进`docs/STATUS.md`当前里程碑中边界清晰、能够验证的一组工作；未通过当前里程碑门禁，不提前实现后续里程碑。
- 保持依赖单向：机理模型不得导入RTO；RTO核心只依赖中立合同和仿真端口；只有专用CDU适配器可以导入 `petroleum_rto.cdu.runtime`。
- 优化目标、优先级和策略规则不得进入机理方程或M7运行合同。机理模型只接收上下文、物理参数、设定值或事件并返回物理证据。
- 统一治理对象必须分责：`CapabilityCatalog`保存端到端原子能力；`ContextSchema`只定义受信上下文结构；`SystemPolicy`保存不可由业务意图覆盖的兼容规则、路由、预算和硬门禁；`Recipe`只允许作为可选默认或锁定模板，不能成为执行必填项，也不能扩展前三者的边界。
- 公开能力清单必须由`CapabilityCatalog`、`ContextSchema`和`SystemPolicy`确定性投影得到，并删除公式、模型内部路径和适配器绑定；不得维护一份脱离可执行配置的手写能力白名单。
- 统一 `OptimizationIntent` 和 `OptimizationProblem` 必须接受任意非空目标向量（`1..N`）。目标数量只是问题特征，不能再通过合同版本、workflow分支或强制profile区分所谓“单目标主线”和“多目标主线”。
- 意图解析或规范化只能校验原子能力ID、方向、显式优先级、输出请求和兼容性；不得读取运行上下文、选择求解器、调用仿真，或静默补造业务目标、自由公式和门禁阈值。
- `ProblemBuilder`必须确定性、可测试且不求解：只能在意图成功解析后，把意图、受信 `OperatingContext`、`CapabilityCatalog` 和 `SystemPolicy` 编译成不可变问题，不能根据求解器实现反向改写问题。当前 `UnifiedProblemBuilder` 在入口内调用 `IntentResolver`并且只在`resolved`状态继续；这不代表问题构造器可以吸收业务语义解释职责。
- `FeatureAnalyzer`（当前实现为 `ProblemFeatureAnalyzer`）只能从不可变问题提取目标数、变量数、网格规模、结果形状、预算、梯度和动态复核等事实；不得选择、注册或调用求解器。
- `SolverRouter`只能依据版本化路由政策、问题特征和显式注册表选择兼容 `SolverPort`，或返回结构化不支持结果；受信override也必须经过同一能力检查。路由器不得求解、调用仿真或修改问题。
- `SolverPort`必须通过无副作用的 `supports()`声明能力，并通过统一 `solve(problem, evaluator)`执行；求解插件不得解析业务自然语言、取得上下文事实或直接依赖CDU/HYSYS内部对象。候选评价只能经中立 `CandidateEvaluatorPort`进入统一评价链。
- 所有问题必须先过滤全部系统硬门禁；多个目标需要显式、版本化的选择偏好后才能从Pareto集合选定动作。禁止用未声明权重或综合分数抵消稳定性、质量或其他硬约束失败。
- 垂域模型只能输出业务意图、优先级、原子能力ID和可选 `Recipe` 引用，不能输出运行上下文事实、任意KPI公式、自由门禁阈值、模型内部路径或求解器ID；RTO必须提供公开能力投影和结构化校验错误供上游修复。
- 垂域模型必须通过版本化通信协议提交完整响应，并回显当前请求与公开能力指纹；合同或关联错误只允许按协议进行有界完整重生成，禁止局部JSON Patch、无限自动重试或绕过`IntentCommunicationService`直接调用`ProblemBuilder`。
- 用户澄清问题必须由结构化歧义和当前公开能力确定性生成；回答只能修复对应问题，不能把垂域模型推断升级为运行上下文事实。只有通信结果为`resolved`时才能进入受信Context绑定。
- 决策变量必须来自版本化 `CapabilityCatalog`，并在内部声明角色、单位、局部域、M2/M4映射和控制器所有权。不得把M7输入白名单或M6适用域直接当成RTO安全边界。
- RTO输出中立的绝对高层稳态设定值及证据引用，不输出模型内部字段、摄氏/表压直接比值、底层阀位或未经支持的分段轨迹。
- 每个候选必须与同上下文基准配对。M2配对只允许决策参数不同；M4配对必须从相同基准参数和初态开始，只允许候选设定值事件不同，且基准要显式取消preset默认扰动。
- 统一评价器只读仿真证据，负责KPI提取、约束裕度、基准差值和配对一致性；不得重复求解、覆盖原始证据或用低优先级收益抵消硬约束失败。
- 系统、资源、I/O、合同或适配器错误不得标成工艺不可行，也不得参与候选排序；错误分类和重试必须遵守RTO现行主文档与版本化政策。
- 策略payload和release不可变，生命周期变化使用append-only事件；未经完整评价与离线人工审核不得发布。失败候选首版只保留评价和运行证据，不生成负案例策略条目。
- 优先复用现有设计、配置和证据。任何新假设、合成阈值或局部范围都要记录来源、版本、适用范围和不确定性。
- 不得用现场数据不足解释程序内部的不守恒、非有限值、负库存、错误收敛状态、错误配对或证据不闭合。
- 未通过当前执行门禁时，不安装依赖、不运行测试或仿真，也不把环境标记为就绪。
- 不覆盖无关的用户修改；发现与当前任务冲突的既有改动时，暂停并说明冲突。

## STATUS 实时更新协议

`docs/STATUS.md` 必须根据项目实际进度实时维护，不能只在整个里程碑结束时更新。

出现以下任一情况后，应在同一工作轮次立即更新状态文档：

- 新增、修改、移动或删除了实施相关的代码、配置、测试、合同或文档；
- 某项交付物开始、完成、退回或被阻塞；
- 执行了测试、仿真、性能检查、校准、链接检查或端到端验证并获得新结果；
- 发现状态记录与仓库实际不一致；
- 用户改变了目标、范围、优先级、架构、环境门禁或验收要求；
- 工作即将暂停、交接或结束。

每次更新必须基于当时可以检查的事实，并至少同步：

1. 最后更新时间和当前里程碑；
2. 当前执行状态及门禁；
3. 本轮实际变更和受影响文件；
4. 已执行的验证、结果和证据；
5. 未执行的验证及原因；
6. 新增阻塞、风险、假设或待决事项；
7. 一个明确、可执行且仍在授权范围内的下一步；
8. 简明的状态变更记录。

状态判断遵循：

- 仓库实际内容优先于旧状态描述，验证证据优先于主观判断。
- 只有满足现行实施方案的验收条件并有证据时，里程碑才能标记为完成。
- 代码存在但没有运行，只能标记为“已实现、未验证”或“部分完成”。
- 测试失败必须如实保留；未执行验证必须写明“未执行”和原因。
- 不使用没有明确计算口径的完成百分比。
- 顶部快照始终反映最新事实，历史记录保持简明且不冒充当前状态。

## 验证与完成门槛

- 环境门禁解除后，先执行与变更最相关的最小验证，再逐步扩大到模块、端到端和全仓库回归。
- 每个当前里程碑必须按 `docs/STATUS.md` 指向的现行方案逐项确认；R0～R11既有交付继续作为迁移回归基线，不得用统一化新功能掩盖旧门禁失败。
- 合同严格性、单位换算、规范指纹、候选去重、基准配对、失败语义、硬约束优先级、证据可重载和结果重复性必须有自动反例。
- 任何机理模型改动都必须继续满足守恒、边界值、方向性、收敛失败处理和既有M0～M7回归。
- 运行产物必须区分合成仿真数据、弱时间对齐现场资料和未来现场在线数据，不得提升证据声明范围。
- 只有代码、配置、测试、文档、运行证据和 `docs/STATUS.md` 相互一致时，才能宣布本轮或里程碑完成。

## 结束与交接

结束或暂停一轮工作前，必须：

1. 复核本轮涉及的文件、工作树范围和验证结果；
2. 按实际情况更新 `docs/STATUS.md`；
3. 确认状态中的阻塞项、待决事项和下一步足以让下一位Agent直接继续；
4. 在交付说明中报告已完成内容、未执行验证、风险和下一步，并与状态文档保持一致。
