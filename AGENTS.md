# Codex 项目工作协议

## 长期目标与声明边界

- 本项目建设可独立验证、可组合调用的石油加工机理仿真、离线RTO、评价、策略和垂域意图部件。
- 业务意图、受信运行事实、优化问题、求解、机理证据、政策评价和策略治理必须保持清晰分责。
- 上层模块不得依赖具体仿真后端、求解器或供应商的内部对象；跨模块替换能力只有在稳定端口和实际替代验证成立后才能宣称完成。
- 未获得现场证据和正式授权前，所有结果只属于合成工程仿真，不得表述为现场验证、产品放行、实际收益、安全边界或可直接下装的控制策略。

## 最小充分工程原则

- 只实现当前明确需求，并使用满足正确性、安全性、可读性和可维护性的最小充分复杂度。
- 决策优先级依次为：正确性；真实信任边界上的安全与数据完整性；简单性与局部可理解性；可维护性；最后才是可扩展性。
- 优先直接、显式、朴素的实现。避免为设计模式、目录美观或假想未来需求增加层、包装器、工厂、注册表、插件系统、配置项、依赖或兼容路径。
- 只有出现真实重复、独立变化方向、多个实现、明确替换需求或必要测试边界时才提取抽象；通常以三次真实重复作为抽象信号，而不是机械规则。
- 单一实现默认不需要接口。保留接口必须能指出当前的替换、隔离或测试理由。
- 配置即合同。没有运行时消费者的字段不得伪装成可执行能力；一项行为只能有一个权威事实源。
- 兼容性只保护当前明确支持的公共合同。没有实际消费者的旧writer、runner、入口、配置、测试和迁移层应删除；Git历史不是运行时兼容层。
- 先隔离再删除，按可达性而不是目录名判断去留。不要重构即将删除的代码。
- 在文件、网络、反序列化、不可信模型输出、外部适配器和持久化读取等真实边界严格校验；经过构造的不可变内部对象应可信传递，不重复证明同一不变量。
- 错误只在能够增加上下文、恢复、转换领域语义或保护边界的位置处理。安全相关分类必须结构化，不能依赖异常消息文本。
- 每次变更前进行删除检验：若移除某个新层、字段、选项或机制不会破坏当前需求，就不应加入。
- 同样正确的方案中，选择概念、层、依赖、配置点和隐藏行为更少的方案。不得以“以后可能需要”为由预付复杂度。

## 文件职责与事实优先级

- `docs/STATUS.md`是当前进度、授权、门禁、阻塞和下一步的唯一事实来源。
- 项目导航与目录边界以`docs/README.md`和架构主文档为准。
- 各模块的现行综合说明负责当前技术合同、能力和限制；归档材料只解释历史背景，不定义当前行为。
- 可执行字段、版本和数值以源码及版本化配置为准；Markdown负责解释，不复制维护第二套配置。
- 本文件只保存长期有效的工作规则，不记录里程碑状态、临时问题、具体版本、运行结果、当前类名、字段清单或某次实施方案。
- 当文档相互冲突时，优先级为：可检查的源码与配置、自动验证和正式证据；`docs/STATUS.md`；现行主文档；历史归档。

## 开始工作前

1. 阅读`docs/STATUS.md`，确认当前授权、门禁、阻塞和下一步。
2. 阅读当前工作涉及的现行模块主文档；只有追溯历史决策时才读取归档。
3. 检查相关源码、配置、测试、产物和工作树，核对状态文档与仓库事实。
4. 若状态与仓库不一致，先按可检查事实修正`docs/STATUS.md`，再实施。
5. 确认工作未扩大外部系统、依赖、数据用途、现场能力或用户授权；需要扩大时先取得确认。

计划、待办、文档或未执行的代码不能证明完成。代码已写但未运行，只能记录为“已实现、未验证”。

## 长期架构边界

- 目录采用类型分层和模块命名空间；代码、配置、数据、测试、脚本、报告与产物必须有明确模块归属。
- 依赖保持单向：机理模型不依赖RTO；RTO核心只依赖中立合同和端口；具体后端依赖只存在于专用适配器；垂域模型不依赖求解器或仿真内部对象。
- 业务意图不得携带或生成受信运行事实、任意公式、自由门禁、内部路径或算法选择。
- 问题构造必须确定性且不求解；求解器只消费不可变问题并通过统一评价边界取得候选证据。
- 每个候选必须与同上下文基准配对。硬约束和系统错误不能被目标收益抵消，也不能伪装成工艺不可行。
- 机理模型只产生物理证据；评价器只解释证据；策略只引用经过完整评价和审核的结果。任何层都不得覆盖原始证据。
- 持久化产物必须支持严格读取、完整性校验和确定性重建；完整性哈希不等同于身份认证或恶意防篡改证明。
- 策略与发布记录保持不可变和追加式演化；没有真实授权边界时不得用内部状态冒充身份、审批或现场控制权。
- 不安装或引入新依赖、后台服务、数据库、队列、缓存、并发框架、插件平台或现场接口，除非当前需求明确需要且门禁已授权。
- 不覆盖无关用户修改；遇到冲突时暂停并说明。

## 实施与验证

- 每次只推进边界清晰、能够独立验证的一小批工作；先做最相关的最小验证，再扩大到模块、端到端和全仓回归。
- 删除旧能力时同步删除其入口、导出、配置、数据、测试、文档和产物引用；不得留下不可达的半兼容层。
- 重构必须保持现有外部行为，除非用户明确授权合同或行为变化；合同变化必须显式升级版本，不能在旧版本下静默改变语义。
- 物理模型变更必须继续检查守恒、边界、方向性、收敛和失败语义。
- RTO变更必须检查合同严格性、单位、问题确定性、路由、候选去重、基准配对、硬约束、错误分类、证据重载和结果重复性。
- 垂域模型变更必须检查外发最小化、凭据保护、请求关联、有界修复、确定性澄清和不可越权调用。
- 测试数量不是目标；保留保护当前行为和真实失败模式的测试，随已删除功能一起删除只维护休眠能力的测试。
- 只有源码、配置、测试、文档、运行证据和`docs/STATUS.md`一致时，才能宣布本轮完成。

## STATUS实时更新

出现实施文件变化、验证结果、范围变化、新风险、阻塞、暂停或交接时，必须在同一轮更新`docs/STATUS.md`。更新至少包含：

1. 当前里程碑、授权和执行状态；
2. 本轮实际变更及受影响范围；
3. 已执行验证及结果；
4. 未执行验证及原因；
5. 风险、假设、阻塞和待决事项；
6. 一个仍在授权范围内的明确下一步；
7. 简明状态变更记录。

失败和未执行验证必须如实保留，不使用没有计算口径的完成百分比。

## 结束与交接

结束或暂停前必须复核工作树和验证结果、更新`docs/STATUS.md`，并在交付说明中明确：已完成内容、删除内容、未执行验证、风险和下一步。交付说明必须与状态文档一致。
